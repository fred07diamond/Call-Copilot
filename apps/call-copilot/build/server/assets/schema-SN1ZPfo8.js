import { a as getDialect } from "./client-BpA2t7pN.js";
import { l as sql } from "./sql-D8aUs1Ib.js";
import { c as text$1, f as integer$1, h as boolean, n as pgTable } from "./table-C1uGOHxK.js";
import { i as integer$2, n as sqliteTable, r as text$2 } from "./table-CK5By4Er.js";
//#region ../../node_modules/.pnpm/@agent-native+core@0.14.8_d8cd0b9ea502c97d2f2b6c28b2fc9a81/node_modules/@agent-native/core/dist/sharing/schema.js
/**
* Schema factories for sharing/ownership.
*
* Templates make a resource table "ownable" by spreading `ownableColumns()` into
* the table definition and declaring a companion shares table via
* `createSharesTable()`. The shares table carries per-principal grants; the
* parent table's `visibility` column is the coarse default (private / org /
* public).
*
*   import { table, text, ownableColumns, createSharesTable } from
*     "@agent-native/core/db/schema";
*
*   export const decks = table("decks", {
*     id: text("id").primaryKey(),
*     title: text("title").notNull(),
*     data: text("data").notNull(),
*     ...ownableColumns(),
*   });
*
*   export const deckShares = createSharesTable("deck_shares");
*
* The share row `role` supports `viewer | editor | admin`. `admin` is a
* share-level capability — it lets non-owners manage shares. The single
* `owner_email` column on the resource remains the ground truth for ownership.
*/
/**
* Columns any ownable resource should include. Spread into the table definition.
*
* - `ownerEmail` — the creator. Auto-filled by write actions from the request context.
* - `orgId`     — owner's active org at creation time. Used for `visibility = 'org'` checks.
* - `visibility`— coarse default: `'private' | 'org' | 'public'`. Default `'private'`.
*/
function ownableColumns() {
	return {
		ownerEmail: text("owner_email").notNull().default("local@localhost"),
		orgId: text("org_id"),
		visibility: text("visibility", { enum: [
			"private",
			"org",
			"public"
		] }).notNull().default("private")
	};
}
/**
* Create a companion shares table for an ownable resource. Call this right
* after defining the parent table.
*
* Columns:
* - `id`              — share row id (generated by the share action)
* - `resourceId`      — id of the parent resource
* - `principalType`   — `'user' | 'org'`
* - `principalId`     — email (for user) or org id (for org)
* - `role`            — `'viewer' | 'editor' | 'admin'`
* - `createdBy`       — email of the user who created the share
* - `createdAt`       — ISO timestamp
*
* Uniqueness across `(resource_id, principal_type, principal_id)` is enforced
* at the action layer via upsert-style logic. A composite DB-level unique
* index can be added per template if desired — omitted here to keep the
* factory dialect-agnostic.
*/
function createSharesTable(tableName) {
	return table(tableName, {
		id: text("id").primaryKey(),
		resourceId: text("resource_id").notNull(),
		principalType: text("principal_type", { enum: ["user", "org"] }).notNull(),
		principalId: text("principal_id").notNull(),
		role: text("role", { enum: [
			"viewer",
			"editor",
			"admin"
		] }).notNull().default("viewer"),
		createdBy: text("created_by").notNull(),
		createdAt: text("created_at").notNull().default(now())
	});
}
/** Role precedence — higher number = stronger capability. */
var ROLE_RANK = {
	viewer: 1,
	editor: 2,
	admin: 3,
	owner: 4
};
//#endregion
//#region ../../node_modules/.pnpm/@agent-native+core@0.14.8_d8cd0b9ea502c97d2f2b6c28b2fc9a81/node_modules/@agent-native/core/dist/db/schema.js
/**
* Dialect-agnostic Drizzle schema helpers.
*
* Templates import `table`, `text`, `integer`, and `now` from here instead of
* importing directly from `drizzle-orm/sqlite-core` or `drizzle-orm/pg-core`.
* The correct dialect is chosen at runtime based on `DATABASE_URL`.
*
* Usage:
*   import { table, text, integer, now } from "@agent-native/core/db/schema";
*
*   export const users = table("users", {
*     id: text("id").primaryKey(),
*     name: text("name").notNull(),
*     active: integer("active", { mode: "boolean" }).notNull().default(true),
*     createdAt: text("created_at").notNull().default(now()),
*   });
*/
function pg() {
	return getDialect() === "postgres";
}
/**
* Define a table. Delegates to `pgTable` or `sqliteTable` based on dialect.
*/
var table = ((...args) => pg() ? pgTable(...args) : sqliteTable(...args));
/**
* Text column. Works identically in both dialects.
* Supports `{ enum: [...] }` config in both.
*/
var text = ((...args) => pg() ? text$1(...args) : text$2(...args));
/**
* Integer column.
*
* Handles `{ mode: "boolean" }` transparently — maps to Postgres `boolean`
* type when running against Postgres, and SQLite `integer` with boolean
* coercion when running against SQLite.
*/
var integer = ((...args) => {
	if (pg() && args[1]?.mode === "boolean") return boolean(args[0]);
	return pg() ? integer$1(...args) : integer$2(...args);
});
/**
* Dialect-agnostic "current timestamp" SQL expression.
* Use with `.default(now())` on text columns storing timestamps.
*
* - Postgres: `now()`
* - SQLite:   `(datetime('now'))`
*/
function now() {
	return pg() ? sql`now()` : sql`(datetime('now'))`;
}
//#endregion
export { ROLE_RANK as a, text as i, now as n, createSharesTable as o, table as r, ownableColumns as s, integer as t };
